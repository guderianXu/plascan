# Temple 深度表面连续性与纹理质量优化计划

**目标：** 在继续使用“深度图直接生成 TSDF 三维模型”的前提下，解决 Temple 细柱和横梁的局部缺面、
开放毛边及顶点色错位。优先恢复有跨视证据但未形成网格的真实表面，不使用密集点云中间步骤，不通过
全局膨胀、无条件插值或大孔封口伪造几何。

**执行环境：** Windows 原生 PowerShell；只使用
`E:\code\plascan\build\windows-vcpkg-cuda-release`；MVS 测试统一放在
`src/core/mvs/tests`；Python 只使用仓库根目录 `.venv`；不 reset、checkout、clean、回滚、commit 或 push。

---

## 1. 当前基线与剩余根因

最新 Temple Ultra 384 基线：

- coverage：`0.94975`，已达到 `0.90` 门槛；
- silhouette IoU：`0.88763`，距离 `0.90` 还差 `0.01237`；
- edge P90：`12.48 px`，目标为 `3 px`；
- foreground SSIM：`0.61545`，目标为 `0.75`；
- 网格为一个连通分量，无漂浮分量；
- 13 个 accepted 深度帧、3 个 validation-only 深度帧；
- 三源跨视补回 `4193` 个像素。

剩余问题不是统一的一像素边缘偏移：各视角 edge P50 只有 `1–2 px`，但
`templeSR0014` 的 edge P90 达到 `30.41 px`。长尾来自少数局部大缺面。

逐阶段归因表明存在两类不同问题：

1. `templeSR0012/0014/0016` 的缺失区域主要没有达到强几何支持；
2. `templeSR0009` 有 `3128/3962` 个缺失像素已经具有可靠深度，但没有形成 TSDF 表面。

纹理方面，`680460` 个顶点中只有 `347401` 个获得可靠多视图颜色，`325776` 个依赖最佳单视图回退，
因此 SSIM 不能只靠继续补几何解决。

---

## 2. 验收原则

每项优化必须在同一 MVS workspace 上执行开关 A/B，不允许用两次独立 SfM/MVS 重跑的随机差异替代
对照。优先级依次为：

1. 不生成柱间薄片、门洞封口和漂浮分量；
2. 降低 edge P90 和开放边界；
3. 提高 IoU 与 coverage；
4. 最后优化纹理 SSIM。

几何改动的最低接受条件：

- component count 保持 `1`；
- largest component face ratio 保持 `1.0`；
- floating diagonal ratio 保持 `0`；
- coverage 不得下降超过 `0.002`；
- IoU 不得下降；
- edge P90 至少改善 `0.5 px`，或开放边界减少至少 `5%`；
- 不接受仅增加面数、同时增加开放边界的方案。

在继续使用 `0.90 / 3 px / 0.75` 严格门槛前，应把 Metashape Temple 模型放入同一相机、同一前景掩膜、
同一分辨率的 `model_quality_cli` 中运行一次。若 Metashape 也不能达到某项门槛，应记录参考模型实测值，
区分“算法尚未达到参考质量”和“门槛本身不可达”，但不能为了让 PlaScan 通过而直接放宽门槛。

---

## 3. 阶段一：把跨视支持从计数升级为可追踪证据

### 3.1 产物扩展

当前 `raw_geometry_support_path` 只有支持数量，无法判断两个相邻像素是否由相同相机集合确认。新增紧凑
证据产物：

- `raw_geometry_source_mask_path`：最多 16 个源视图的 bit mask；
- `raw_inverse_depth_mean_path`：一致候选的逆深度均值；
- `raw_inverse_depth_spread_path`：一致候选的稳健离散度；
- `cross_view_repaired_mask_path`：明确标记补回像素；
- manifest 记录每项产物路径、版本和统计直方图。

若视图超过 bit mask 容量，只记录实际 source plan 的局部编号，不保存全工程帧号位图。

### 3.2 单元测试

在 `src/core/mvs/tests` 增加测试，验证：

- 同一深度簇正确合并 source ID，重复源视图不能重复计数；
- 前景/背景两个逆深度模式不会被平均；
- source mask、均值和离散度写盘后可无损读取；
- 老 workspace 缺少新产物时保持当前保守行为。

---

## 4. 阶段二：从三源强种子向两源弱支持区域受控生长

### 4.1 适用范围

只处理一致性过滤后为空、但有两个独立源视图形成稳定逆深度簇的像素。不能把所有两源像素直接恢复。

### 4.2 生长约束

两源候选必须同时满足：

- 与一个三源或更多支持的强种子位于同一连通区域；
- source ID 集合与强种子至少共享一个真实源视图；
- 逆深度相对离散度不超过 `1.0%–1.5%`；
- 与强种子的局部法线夹角不超过 `15°–20°`；
- 图像引导边缘代价没有跨越强梯度；
- 到强种子的 geodesic 距离不超过 `3–5` 像素；
- 不接触 support mask 外部，不跨越深度断层；
- 每个连通补回区域有面积上限，超限区域只保留三源核心。

生长使用优先队列按“支持数、逆深度离散度、法线一致性、图像边缘代价”排序，不使用普通形态学膨胀。

### 4.3 参数 A/B

在同一 Temple 深度 workspace 上至少测试：

- geodesic 半径：`3 / 5`；
- 逆深度离散度：`1.0% / 1.5%`；
- 法线阈值：`15° / 20°`；
- 最大补回连通区域：`32 / 64 / 128` 像素。

若 edge P90 没有改善或开放边界增长超过 `3%`，停止扩大生长范围。

---

## 5. 阶段三：TSDF 从单体素计数升级为局部表面片支持

### 5.1 解决 `templeSR0009` 的可靠深度未成面

保留默认两个实际相机观测规则，但为以下情况增加局部表面片准入：

- 当前样本至少有两个真实 source ID；
- 逆深度离散度低；
- 邻域 `3x3` 或 `5x5` 内存在已通过双相机 TSDF 的核心体素；
- 样本法线与核心表面法线一致；
- 候选只扩展到核心表面的一个体素邻域；
- free-space 证据没有明确否定该表面。

局部表面片支持不能提高深度帧的 accepted/validation-only 等级，也不能形成没有强核心连接的新组件。

### 5.2 统计与诊断

TSDF JSON 增加：

- `surface_patch_recovered_sample_count`；
- `surface_patch_rejected_normal_count`；
- `surface_patch_rejected_source_overlap_count`；
- `surface_patch_rejected_free_space_count`；
- `surface_patch_created_component_count`；
- 补回前后 boundary edge 数和弱支持面数。

质量报告继续使用 `missing_stage.png`，并新增“可靠深度被 TSDF 拒绝”与“表面片补回成功”两种颜色。

### 5.3 回退条件

出现以下任一情况立即关闭该实验：

- 新增第二个大连通分量；
- 门洞或柱间出现连通薄片；
- boundary edge 增长超过 `3%`；
- IoU 下降超过 `0.001`；
- `templeSR0014` edge P90 继续上升。

---

## 6. 阶段四：边缘长尾专项优化

对四个验收视角分别输出：

- reference edge；
- rendered edge；
- 双向距离图；
- P90 以上像素掩膜；
- 对应的深度支持数、source ID、逆深度离散度和 TSDF 拒绝原因。

首先处理 `templeSR0014` 的 `30.41 px` 长尾区域。只有确认缺口属于同一真实柱面时，才允许阶段二或阶段三
补回；如果长尾来自前景掩膜边缘偏差，应修正掩膜，不修改深度或网格。

目标是先把每个视角的 edge P90 控制到 `10 px` 以下，再尝试整体 `5 px` 和最终 `3 px`。不使用一次性
大幅放宽支持阈值追逐中位数。

---

## 7. 阶段五：纹理和 SSIM 单独优化

### 7.1 先校准评估对象

当前质量 CLI 渲染 PLY 顶点色，而最终 OBJ 已有相机投影纹理图集。增加纹理模型渲染或将相同纹理采样
结果转换为质量 CLI 可读输入，分别报告：

- PLY vertex-color SSIM；
- OBJ camera-atlas SSIM；
- 几何公共区域 SSIM；
- 曝光补偿前后 SSIM。

不能用纹理结果掩盖几何缺失；SSIM 只在 reference/render 公共前景上计算。

### 7.2 颜色优化

- 为每个相机估计稳健增益和偏置，统一曝光；
- 每个面优先使用视角、分辨率和遮挡最优的一个主视图；
- 只在几何接缝附近做多带融合，避免整面多视平均导致模糊；
- 对最佳单视图回退顶点记录来源和置信度；
- 禁止颜色传播跨越法线断层或真实开口。

纹理阶段目标：先使 camera-atlas SSIM 超过当前 `0.61545` 至少 `0.05`，再评估 `0.75` 是否可达。

---

## 8. 实施顺序与测试

1. 增加 source ID、逆深度均值/离散度产物和读写测试；
2. 完成 P90 长尾诊断图，确认 `templeSR0014` 的空间位置；
3. 实现两源区域从三源核心受控生长；
4. Temple 同 workspace A/B，不达标立即回退实验开关；
5. 实现 TSDF 局部表面片支持；
6. Temple 同 workspace A/B，选择边界增长最小的配置；
7. 完成纹理模型质量评估和曝光补偿；
8. 编译 GUI/CLI，并运行：
   - `test_mvs_cross_view_hole_repair`；
   - `test_mvs_depth_completeness`；
   - `test_mvs_pipeline`；
   - `test_mvs_types`；
   - `test_mesh_reconstructor`；
   - `test_model_image_quality`；
   - `test_cli_contracts`；
9. 仅使用 Temple 完成最终 highest + Ultra 384 回归，保存模型、质量报告、对照表和诊断图。

---

## 9. 阶段完成标准

本阶段不以“代码已编译”作为完成。必须同时满足：

- Temple 模型仍为一个主连通分量且无漂浮分量；
- coverage 保持不低于 `0.947`；
- IoU 达到或超过 `0.90`；
- edge P90 先降到 `10 px` 以下，并提供继续到 `3 px` 的剩余归因；
- camera-atlas SSIM 明显高于当前 PLY 顶点色 SSIM；
- 柱间开口保持开放，细柱缺口和毛边肉眼可见减少；
- 所有新增路径有关闭开关，旧 workspace 可继续加载；
- 相关测试全部通过，文档记录被接受和被拒绝的 A/B 参数。

---

## 10. 2026-07-20 执行结果

- 已完成来源相机 bit mask、逆深度统计、跨视图修补掩膜的持久化、旧工程兼容读取和单元测试。
- 已完成逐视图 Edge P90 长尾诊断。Temple 最差长尾主要是模型在真实开口内新增的边缘，并非统一的
  外轮廓平移。
- 两来源受控生长在 Temple 上没有合格候选；TSDF 局部表面片补回会增加开放边界；强制保留覆盖
  崩塌的 Level 1 会把 Edge P90 推高到 `25 px`。三项均未设为默认。
- 关闭补洞、禁用单视图 TSDF、3/6 体素支持带、5 体素截断带和 3% 深度一致性阈值均未带来净收益。
- 接受“环拍物体五视图支持掩膜自由空间共识”：只有至少五个参考相机一致判为背景才以 `0.25`
  权重雕刻；航测和旧工作区默认关闭，用户可显式覆盖。
- 最终 highest 320 默认回归：coverage `0.95419`、IoU `0.89445`、edge P90 `12.65 px`、SSIM
  `0.61454`。Ultra 384 全 16 视图：coverage `0.95235`、IoU `0.90611`、edge P90 `12.50 px`、
  SSIM `0.61587`。模型保持一个主连通分量，IoU 目标已达到；edge 和 SSIM 仍未达到严格门槛。
- 下一阶段应针对最差视图的深度来源选择和纹理曝光/主视图分配继续优化，不再通过全局放宽或收紧
  TSDF 支持阈值追逐指标。

## 11. 2026-07-21 源视图与颜色回归

- 验证优先的源视图规划不再允许 `geometric_inliers=0` 的 track-geometry 回填；证据不足时保留
  source shortfall，不再把仅有投影重叠的视图送入 PatchMatch。源规划测试 `14/14` 通过。
- TSDF 清理末尾新增无损引用顶点压缩。旧 Temple Ultra 网格删除 `5920` 个未被任何三角面引用的顶点，
  面、边界和主连通分量不变；网格/TSDF/纹理测试 `59/59` 通过。
- 修复最佳单视图颜色回退绕过曝光增益的问题，但 A/B 表明 Temple 默认全局曝光补偿会使同配置 SSIM
  从 `0.62007` 降至 `0.58437`。因此保留显式 `tsdfCompensateColorExposure` 开关，默认关闭；后续改为
  按面主视图与接缝局部融合，不采用全局亮度归一化。
- 使用 Middlebury 官方 16 相机参数的独立 highest/CUDA 回归中，16 帧全部 accepted，ref 5 有效区覆盖
  为 `0.9865`，源计划无零内点项；模型保持一个主连通分量。质量为 coverage `0.94542`、IoU
  `0.89254`、edge P90 `12.78 px`、SSIM `0.60938`，严格门仍未通过。由于相机来源与旧工程 SfM
  相机不同，本次 ref 5 改善不能单独归因于源视图门修复。
- 直出 TSDF 仍未遵守 `240000` 目标面数；现有简化器是体素聚类而非边缘保持 QEM，不能直接设为默认，
  否则可能进一步磨损细柱。该项继续作为独立实现任务。

## 12. 2026-07-21 QEM 与按面颜色 A/B

- 新增带开放边界保护、锐边拒绝、link-condition 和翻转检查的批量 QEM 实验路径，使用
  `tsdfQuadricSimplification=true` 显式启用。Temple 从 `1263915` 面降至 `387722` 面，但受保护约束限制
  未达到 `240000` 面，且开放边界由 `48924` 增至 `54366`（`+11.1%`）。同配置质量由 coverage
  `0.95629`、edge P90 `9.78 px`、SSIM `0.62007` 退化至 `0.95504`、`10.09 px`、`0.52738`，
  因此默认关闭。
- 新增 `tsdfCoherentFacePrimaryViewColors=true` 实验路径：每个面先选择一个主相机，再由相邻面主相机
  票数决定共享顶点颜色。Temple 有 `936346` 个面和 `504756` 个顶点获得一致主视图，但 PLY SSIM
  从 `0.62007` 降至 `0.60396`，因此默认关闭。PLY 共享顶点不能表达真正的逐面纹理接缝，下一步应
  让 GUI 直接加载已有 OBJ/MTL/纹理图集，而不是继续改变默认顶点色。

## 13. 2026-07-21 GUI OBJ/MTL 纹理显示链路

- 已让生成模型入口默认请求 OBJ；底层仍无条件保存 `model_from_mesh.ply`，OBJ/MTL/PNG 失败时自动
  回退 PLY。项目结果增加 `textured` 和 `texture_image`，工作区以 `final_model_path` 优先显示 OBJ。
- `CameraSceneWidget` 的 OBJ 后台任务现在同时解析面级 UV、MTL `map_Kd` 和纹理图。Vulkan 新增纹理
  网格管线，按每个三角形角点的 `faceTextureIndices` 采样，不再把同一空间顶点强制合并为一个颜色。
  MTL、纹理或完整 UV 缺失时继续使用原有顶点颜色管线。
- Temple 功能回归
  `E:\code\test\temple\regression\20260721_obj_mtl_gui_validation_run2` 使用 15 个深度帧生成
  `56362` 顶点、`112289` 面；OBJ 含 `336867` 个纹理坐标（每面 3 个），映射面 `112289`，未映射面
  `0`，MTL 正确引用 `4096×4096` 的 `textures/model_texture.png`。此 128 分辨率运行只验证产物和显示
  链路，不替代 Ultra 384 的几何质量门回归。

## 14. 2026-07-21 TSDF 柱边亚像素观测 A/B

- Temple Ultra 384 的 `3517840` 次 TSDF 观测在投影最近像素无效时从相邻像素借值；该路径会在细柱、
  门洞和深度孔洞边缘横向延伸表面。新增跨视图逆深度离散门、邻借值开关及对应统计，旧分辨率和旧
  workspace 保持兼容。
- 同一 MVS workspace A/B 中，3% 全局跨视图硬门会被邻借值路径部分绕过，并使开放边界从 `41052`
  增至 `41141`；1.5%/2.0% 仅限制不稳定邻借值也分别增加至 `41659`/`41408`，均不设为默认。
- Ultra 禁止无效最近像素邻借值后，开放边界降至 `40712`，保持一个主连通分量；16 视图投影质量由
  coverage `0.95533`、IoU `0.90915`、edge P90 `10.00 px`、SSIM `0.63823` 变为 `0.95365`、
  `0.90832`、`9.41 px`、`0.63814`。边缘长尾改善而覆盖损失小于 `0.2` 个百分点，因此仅对 Ultra
  384 设为默认，320 及以下继续允许邻借值。
- 当时使用固定支持阈值 `4` 的缺失归因显示约 `70%` 的模型缺口没有可靠深度；后续阶段 15 已确认
  该诊断会误判只有两个源视图的帧，因此不得用它判断 MVS/TSDF 责任比例。

## 15. 2026-07-21 自适应几何支持门与细柱补全

- 修复质量诊断把所有帧的可靠几何支持固定判为 `>=4` 的问题。阈值现在取
  `min(4, source_view_count + 1)`，两源帧使用 `3`；Temple 后 5 帧的可靠深度覆盖实际为
  `85.6%–95.6%`，不再被错误报告为 `0%`。
- 修正后，Temple 模型缺口中“没有可靠深度”的中位占比为 `41.3%`，“已有可靠深度但 TSDF 缺失”
  为 `58.7%`，主要阻塞位于 TSDF 单参考帧支持门，而非 MVS 全局覆盖率。
- Ultra 384 将几何确认单参考帧的最小支持数从 `4` 调整为 `3`，同时保留高置信度、至少两源几何
  确认、禁止无效最近像素邻借值和双相机 TSDF 优先等约束。Temple 同 workspace A/B 从
  coverage `0.95365`、IoU `0.90832`、edge P90 `9.41 px`、SSIM `0.63814` 改善为 `0.95491`、
  `0.90937`、`9.27 px`、`0.63893`；主连通分量保持为 `1`。
- 直接用跨视图逆深度均值替换参考深度会把面数从约 `117` 万增至 `138` 万、开放边界增至 `56819`，
  表明不同参考帧的均值不对称并会制造粗糙双层表面，因此保持显式开关且默认关闭。
- 将最小几何支持继续从 `3` 降到 `2` 仅把 coverage 从 `0.95491` 变为 `0.95497`、SSIM 从
  `0.63893` 变为 `0.63903`，IoU 和 edge P90 在有效精度内不变，同时开放边界由 `42996` 增至
  `43149`。收益不足以补偿更弱的几何证据，默认保持 `3`。

## 16. 2026-07-22 柱边终端毛刺清理 A/B

- 对几何确认单参考体素增加“从多视核心邻域生长”的实验门后，拒绝 `162897` 个候选体素，但 Temple
  coverage 从 `0.95022` 降至 `0.94987`，edge P90 从 `8.54 px` 退化至 `8.80 px`；该开关保持默认
  关闭。
- 尝试把 PatchMatch 后处理改为排除无效零深度的相对深度滤波。`2%`、`5%`、`25%` 三档均使通过
  深度质量门的帧由 `15/16` 降至 `13/16`；`2%` 模型开放边界增至 `75764`。该路线已撤销，不进入
  生产默认。
- 原弱边界清理要求三角形的三个顶点全部为弱支持，容易删除连续柱面，却无法递归处理只含一个弱端点
  的叶片状毛刺。新规则只剥离至少含两条开放边且至少含一个弱支持顶点的终端三角形。
- 同一 `E:\code\test\temple\mvs_output`、Ultra 384 A/B 中，两轮终端剥离将开放边界从 `42508`
  降至 `39030`，保持单一主连通分量；配合两轮限位边界平滑后，coverage `0.95028`、IoU
  `0.90971`、edge P90 `8.27 px`、SSIM `0.65288`。相对基线 coverage 略升、edge P90 改善约
  `3.2%`、SSIM 略升，因而只对 Ultra 384 设为默认，其他质量档维持一轮平滑且不启用弱端剥离。
- 一轮 `lambda=0.08` 的全表面 Taubin 平滑会使 coverage 降至 `0.95003`、edge P90 回退至
  `8.30 px`，只有 SSIM 微升至 `0.65298`，收益不足且存在磨损雕刻细节风险，实验实现已撤销。
- 严格质量门仍未通过，剩余主要问题是柱面内部的低频深度起伏和纹理一致性，而非开放边终端毛刺；
  后续不得再靠扩大边界裁剪解决，应采用有多视法线约束的表面去噪并单独验证雕刻细节保留率。

## 17. 2026-07-22 Temple 可视化复核与 OBJ 加载优化

- 对每项实验都使用 `templeSR0015` 的 2K 回投近景复核。受支持 TSDF 微裂缝填补仅把 coverage 从
  `0.95028` 改为 `0.95049`，柱缘画面几乎不变；25% 跨视逆深度收缩的 IoU 微升至 `0.91016`，但
  SSIM 降至 `0.64983` 且毛刺未减少；两项实现均已撤销。
- 把全部完成深度帧作为补洞来源会产生背面误投影，IoU 降至 `0.78396`、edge P90 恶化至
  `48.13 px`，近景出现明显漂浮面；仅加入互选来源又无法为两源帧形成新候选。两条来源扩展均已撤销。
- 现有 `dense_cloud.ply` 的 Poisson 隔离诊断与当前 MVS 相机坐标不一致，回投 coverage 为 `0`，不能
  作为 TSDF 的替代证据。当前可接受几何仍保持阶段 16 的 Ultra 384 结果，严格质量门未通过，不能按
  肉眼质量宣称已解决。
- OBJ 读取改为连续缓冲和无分配数字解析；三角形渲染缓冲与纹理读取移入后台任务。旧 254.25 MB
  Temple OBJ 的解析耗时由 `9127 ms` 降至 `1422 ms`，渲染数据准备 `80 ms`。
- 相机纹理 OBJ 按“主相机 + 顶点”复用 UV。相同 649101 顶点、1292502 面的 Ultra 模型中，`vt` 从
  `3877506` 降至 `1395477`，OBJ 从 254.25 MB 降至 182.96 MB；优化后文件实测解析 `896 ms`、后台
  渲染准备 `87 ms`。几何与逐面主相机选择不变，接缝仍通过不同相机键保留。

## 18. 2026-07-22 当前多视深度复验与缓存失效

- 复核发现项目 `E:\code\test\temple\mvs_output` 的 16 帧深度全部只记录两个源视图，而当前
  环拍最高质量策略会按可验证几何为每帧选择 2–5 个源视图。旧模型反复复用该两源批次，导致此前
  的 TSDF 参数 A/B 无法获得当前多视深度带来的质量改善。
- 使用官方 Temple 16 相机在隔离目录重新生成深度后，16/16 帧 accepted；Ultra 384 模型的 16 视图
  回投由 coverage `0.95028`、IoU `0.90971`、edge P90 `8.27 px`、SSIM `0.65288` 改善为 coverage
  `0.96743`、IoU `0.91846`、edge P90 `6.81 px`、SSIM `0.67474`。接触图确认柱体连续性和开口边缘
  明显改善，但最差侧视图仍有边缘长尾，严格 `3 px / 0.75` 门仍未通过，因此不宣称几何质量已完成。
- 深度产物和配置哈希增加 `algorithm_revision=4`。模型工作流只复用当前修订版的完整深度批次；旧版
  批次仍保留在磁盘，但点击生成模型时会先自动重新估计当前多视深度，避免继续透明复用过时结果。
- 新模型 OBJ（585447 顶点、1134626 面）实测解析 `884 ms`、后台渲染准备 `68 ms`；缓存版本、工作流
  和 OBJ 定向测试通过。
- 对新多视深度关闭支持掩膜自由空间雕刻后，coverage 微升至 `0.96819`，但 IoU 降至 `0.91538`、
  edge P90 退化至 `7.73 px`、SSIM 降至 `0.67402`；接触图没有消除侧视柱缘缺失。因此继续保留
  环拍物体的五视图自由空间共识，不接受该 A/B。
- 新多视深度下再次启用受限局部表面片支持，`336660` 个候选中 `60826` 个因来源重叠不足、
  `275834` 个因权重不足被拒绝，恢复数仍为 `0`；把最低权重比从 `0.10` 降到 `0.05` 结果不变。
  不继续放宽至无可靠权重的补面，基线保持关闭。
